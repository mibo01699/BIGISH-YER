# BIGISH-YER Deployment Environment Configuration
PORT=5000
NODE_ENV=development

# Pi Network Developer Portal Credentials (v2.0 SDK Ecosystem)
PI_API_KEY=pbk_your_secret_pi_core_team_developer_key_here
PI_BLOCKCHAIN_NETWORK=testnet

# Macroeconomic Clearing & Database Parameters
DATABASE_URL=postgresql://user:password@localhost:5432/bigish_yer_ledger
YER_STABILIZATION_RESERVE_LIMIT=10000000
# مفتاح تشفير قاعدة البيانات (يجب أن يتكون من 32 حرفاً عشوائياً في البيئة الحقيقية)
DATABASE_ENCRYPTION_KEY=64_character_hex_string_for_production_safety_key
// مثال لكيفية الاستدعاء داخل كود السيرفر وحفظ ليدجر مشفر بالكامل
const { encryptData } = require('../utils/cryptoEngine');
const secureSenderWallet = encryptData(senderYerWallet); // تشفير محفظة AJYAL
const secureReceiverWallet = encryptData(receiverPosWallet); // تشفير محفظة GAV

