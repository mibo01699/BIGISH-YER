name: BIGISH-YER Ecosystem CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        
    - name: Forced Clean Dependency Installation
      run: |
        rm -rf node_modules package-lock.json
        npm install bignumber.js express axios --no-package-lock --legacy-peer-deps
      
    - name: Execute Clean Sovereign Visa & Gaming Audit
      run: |
        node testVisaRecharge.js
        node testGamingRecharge.js
